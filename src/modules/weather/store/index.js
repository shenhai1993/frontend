import actions from '../store/actions'
import mutations from '../store/mutations'

const state = {
  playlists: [],
  selectWeatherList: [],
  getWeatherPageList: []
}

export default {
  state,
  actions,
  mutations
}
